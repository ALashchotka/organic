import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  container: {
    flexGrow: 0,
  },

  contentContainer: {
    paddingHorizontal: 16,
    paddingVertical: 8,
  },

  separator: {
    width: 8,
  },
});
